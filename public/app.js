const $=s=>document.querySelector(s);
async function loadEngines(){const r=await fetch("/api/engines");const d=await r.json();$("#engineCount").textContent=d.engines.length+" محركات";$("#engines").innerHTML=d.engines.map(e=>`<div class="engine"><i class="${e.configured?"on":""}"></i><div><b>${e.label}</b><small>${e.role}</small></div><span class="status">${e.configured?"متصل":"يحتاج إعداد"}</span></div>`).join("")}
async function loadTasks(){const r=await fetch("/api/tasks");const d=await r.json();$("#tasks").innerHTML=d.tasks.length?d.tasks.slice(0,6).map(t=>`<div class="task"><div><p>${escapeHtml(t.prompt)}</p><small>${t.id.slice(0,8)} · ${new Date(t.createdAt).toLocaleString("ar-EG")}</small></div><span class="badge">${t.engine}</span></div>`).join(""):'<div class="task"><p>لسه مفيش مهام. ابدأ من صندوق الأوامر فوق.</p></div>'}
function escapeHtml(s){return s.replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#039;"}[m]))}
$("#taskForm").addEventListener("submit",async e=>{e.preventDefault();const prompt=$("#prompt").value.trim();if(!prompt)return;const engine=$("#engine").value;const r=await fetch("/api/tasks",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({prompt,engine:engine||undefined})});if(r.ok){$("#prompt").value="";loadTasks()}});
$("#refresh").onclick=loadTasks;loadEngines();loadTasks();
